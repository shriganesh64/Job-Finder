export default theme => ({
	paperContainer: {
		minWidth: '22rem',
		minHeight: '10vh'
	},
	dialogTitle: {
		padding: `${theme.spacing(1)}px ${theme.spacing(2)}px`
	},
	whiteColor: {
		color: theme.palette.common.white
	}
});
